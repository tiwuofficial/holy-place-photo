# holy-place-photo

```
docker exec -it laravel-php php /src/artisan migrate

docker exec -it laravel-php php /src/artisan db:seed

docker exec -it laravel-mysql mysql -ularavel -plaravel holy_place_photo

docker exec -it laravel-php composer dump-autoload

docker exec -it laravel-php /bin/bash
```
